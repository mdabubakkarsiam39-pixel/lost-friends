name: Deploy

on:
  workflow_dispatch:

jobs:
  notify:
    name: Deploy Info
    runs-on: ubuntu-latest

    steps:
      - name: Show deploy instructions
        run: |
          echo "Deployment is managed via Render (backend) and Vercel (web)."
          echo "Push to main branch will auto-deploy via connected services."
